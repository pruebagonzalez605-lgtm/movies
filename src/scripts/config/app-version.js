// Version actual "instalada" de la app empaquetada (Android TV / Capacitor).
//
// IMPORTANTE: cada vez que generes un nuevo APK y publiques un nuevo
// Release en GitHub con ese archivo adjunto, actualiza este numero para
// que coincida EXACTO con el tag_name del Release (ej: "1.31").
// Si no lo actualizas, el modal de "actualizacion disponible" no va a
// saber que hay una version mas nueva.
export const APP_VERSION = "1.0.17";